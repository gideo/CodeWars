// 7kyu - Thinking & Testing: How many "word"

// No Story

// No Description

// Only by Thinking and Testing

// Look at result of testcase, guess the code!

let testit = s => (s.match(/w.*?o.*?r.*?d/ig) || []).length;